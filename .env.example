# 复制本文件为 .env（不要提交到仓库）
# Windows: 直接双击 启动后端.bat 会自动加载 .env

# ==============================
# 推荐：方案 B（OpenAI 兼容中转站）
# ==============================
AI_PROVIDER=openai_compat

# 中转站 API Key（必填；只放在后端）
AI_API_KEY=

# 中转站 Base URL（必填）
# - 可写到 /v1 或不带 /v1 都可以
# - 后端会自动尝试 /models 与 /v1/models
AI_BASE_URL=

# 可选：打开后端调试打印（会打印 response 类型与截断预览）
# AI_DEBUG=1

# ==============================
# 兼容：旧配置键（可不填）
# ==============================
# 说明：若你只填写了 ZHIPUAI_*，后端会在需要时用它们作为 AI_* 的回退。
# ZHIPUAI_API_KEY=
# ZHIPUAI_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
